// validateEmail.js
// Utility function to check if an email address is valid
// - Typically used in NewsletterForm before submission
// - Prevents users from entering incorrectly formatted emails
// - Example: ensures the email contains "@" and a valid domain
//
// Usage:
//   if (validateEmail(email)) {
//     // proceed with form submission
//   } else {
//     // show error message
//   }
